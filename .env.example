# Copie este arquivo para .env e preencha seus valores locais.
# NUNCA commit o arquivo .env (já está no .gitignore).

# Token da API da Render (formato: rnd_...)
RENDER_API_TOKEN=rnd_seu_token_aqui

# (Opcional) Variáveis lidas pelo script de deploy local (PowerShell)
# Inclua o nome do banco e parâmetros recomendados no MongoDB SRV
MONGODB_URI=mongodb+srv://usuario:senha@cluster0.example.mongodb.net/contta?retryWrites=true&w=majority

# Formato recomendado para CloudAMQP (inclui vhost no final)
RABBITMQ_URL=amqps://usuario:senha@host.cloudamqp.com/vhost

# CORS do frontend
CORS_ORIGINS=*.vercel.app,https://localhost:3000

# URL do frontend em produção (opcional)
PRODUCTION_URL=
