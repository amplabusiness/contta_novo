version: "1"
services:
  - type: web
    name: contta-keycloak-staging
    runtime: docker
    plan: starter
  region: oregon
    autoDeploy: true
    rootDir: .docker/keycloak
    dockerfilePath: Dockerfile
    envVars:
      - key: KEYCLOAK_ADMIN
        value: admin
      - key: KEYCLOAK_ADMIN_PASSWORD
        sync: false
    healthCheckPath: /realms/master/.well-known/openid-configuration

  - type: web
    name: contta-searchapi-staging
    runtime: docker
    plan: starter
  region: oregon
    autoDeploy: true
    rootDir: contta-search-api-main/contta-search-api-main
    dockerfilePath: Dockerfile
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 5001
      - key: CORS_ORIGINS
        sync: false
      - key: MONGODB_URI
        sync: false
      - key: OIDC_ISSUER
        sync: false
      - key: OIDC_AUDIENCE
        value: contta-portal
    healthCheckPath: /health

  - type: web
    name: contta-excelparser-staging
    runtime: docker
    plan: starter
  region: oregon
    autoDeploy: true
    rootDir: contta-excel-parser-main/contta-excel-parser-main
    dockerfilePath: Dockerfile
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 5002
      - key: PRODUCTION_URL
        sync: false
      - key: OIDC_ISSUER
        sync: false
      - key: OIDC_AUDIENCE
        value: contta-portal
    healthCheckPath: /health

  - type: worker
    name: contta-consumerxml-staging
    runtime: docker
    plan: starter
  region: oregon
    autoDeploy: true
    rootDir: agendador-back-end-master/agendador-back-end-master/ConsumerXml
    dockerfilePath: Dockerfile
    envVars:
      - key: RABBITMQ_URL
        sync: false
      - key: RABBITMQ_QUEUE
        value: Modelo55
      - key: RABBITMQ_PREFETCH
        value: 20
      - key: RabbitMQ__Durable
        value: true
      - key: RabbitMQ__Exclusive
        value: false
      - key: RabbitMQ__AutoDelete
        value: false
      - key: RabbitMQ__DeadLetterExchange
        value: dlx.nfe
      - key: RabbitMQ__DeadLetterRoutingKey
        value: Modelo55.dlq
