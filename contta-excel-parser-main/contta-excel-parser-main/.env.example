### Excel Parser (.env example)

# Ambiente
NODE_ENV=production
PORT=5002

# CORS (origens permitidas, separadas por vírgula)
# Por padrão, localhost:3000 e *.vercel.app já são permitidos.
CORS_ORIGINS=http://localhost:3000,https://<PORTAL_HOST>

# Autenticação
# Para bypass de auth (sem Keycloak), deixe como true
AUTH_DISABLED=true

# Se for usar OIDC (Keycloak), preencha abaixo e defina AUTH_DISABLED=false
OIDC_ISSUER=https://keycloak.example.com/realms/contta
OIDC_AUDIENCE=contta-apis

# Outros (se necessário)
PRODUCTION_URL=